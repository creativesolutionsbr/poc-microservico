spring:
  application:
    name: discovery-service
  profiles:
    active: local
  cloud:
    config:
      uri: http://localhost:8060
      fail-fast: true
      retry:
        max-attempts: 20
        max-interval: 15000
        initial-interval: 10000
    server:
       port: 8070